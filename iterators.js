// Iterables and Iterators

// String
const str = 'Flowmeter';

for (const char of str) {
  console.log(char);
}

// Array
const arr = ['F', 'l', 'o', 'w', 'm', 'e', 't', 'e', 'r'];

for (const item of arr) {
  console.log(item);
}
